{
	"$schema": "https://docs.renovatebot.com/renovate-schema.json",
	"extends": ["config:recommended", ":semanticCommits", "group:allNonMajor"],
	"schedule": ["before 6am on monday"],
	"commitMessageAction": "bump",
	"commitMessageTopic": "{{depName}}",
	"packageRules": [
		{
			"description": "Patch and minor updates automerge once CI is green. CI runs the unit tests and the gateway tests against the real gateway image in sandbox mode.",
			"matchUpdateTypes": ["patch", "minor"],
			"automerge": true
		},
		{
			"description": "GitHub Actions are pinned to a commit SHA and never automerged, the same rule as proxlane/proxlane.",
			"matchManagers": ["github-actions"],
			"automerge": false,
			"pinDigests": true
		}
	],
	"prConcurrentLimit": 2,
	"prHourlyLimit": 2
}
